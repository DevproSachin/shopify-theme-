<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Shopify theme development</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ content_for_header }}
    {% render 'theme-variable' %}
  </head>
  <body>
    <header>
      <h1>Playground with codersachin shrma.change made now.
      </h1>
      {% sections 'header-group' %}
    </header>
    <main>
      {{ content_for_layout }}
    </main>

    <footer>
       {% sections 'footer-group' %}
    </footer>
  </body>
</html>
